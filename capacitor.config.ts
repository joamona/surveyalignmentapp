import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.surveyalignment.app',
  appName: 'Survey Alignment',
  webDir: 'dist/surveyalignment',
  bundledWebRuntime: false
};

export default config;
